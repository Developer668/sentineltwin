{
  "mcpServers": {
    "cockroachdb-cloud": {
      "type": "http",
      "url": "https://cockroachlabs.cloud/mcp",
      "headers": {
        "Authorization": "Bearer ${COCKROACH_MCP_TOKEN}",
        "X-Cockroach-Cluster-Id": "${COCKROACH_CLUSTER_ID}"
      }
    }
  }
}
